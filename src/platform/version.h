#pragma once
// HELIX_VERSION is defined by CMake from the latest git tag.
// See CMakeLists.txt: add_compile_definitions(HELIX_VERSION="...")
#ifndef HELIX_VERSION
#define HELIX_VERSION "0.0.0"
#endif
